# 환경 설정
python-dotenv>=1.0.0

# 데이터 검증
pydantic>=2.0.0

# HTTP 요청
requests>=2.31.0

# Google API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# 이메일 전송 (Gmail API 사용 시 위 google 패키지로 충분)
# 직접 SMTP 사용 시 추가 패키지 불필요 (표준 라이브러리 사용)
